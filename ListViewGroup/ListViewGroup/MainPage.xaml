<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ListViewGroup"
             x:Class="ListViewGroup.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding ConsoleList}" IsGroupingEnabled="True" GroupDisplayBinding="{Binding Name}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell TextColor="Gray" Text="{Binding Title}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="by Atsumi Amaya" FontSize="Micro"
                   VerticalOptions="EndAndExpand"
                   HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
